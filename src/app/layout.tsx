import type { Metadata } from "next";
import "./globals.css";
import Link from "next/link";
import { getCachedConfig } from "@/utils/data";
import HongKongWeather, {
  HongKongWeatherSkeleton,
} from "@/components/HongKongWeather";
import { Suspense } from "react";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const config = await getCachedConfig();

  return (
    <html lang="en">
      <body>
        <main className="flex min-h-screen flex-col items-center justify-between">
          <header className="w-full bg-white shadow-md">
            <nav className="container mx-auto px-6 py-4">
              <div className="flex items-center justify-between">
                <div className="text-2xl font-bold text-gray-800">MyBlog</div>
                <div>config: {config}</div>
                <div className="flex space-x-4">
                  <Link
                    href="/"
                    className="text-gray-600 hover:text-blue-500"
                    prefetch={false}
                  >
                    Home
                  </Link>
                  <Link
                    href="/about"
                    className="text-gray-600 hover:text-blue-500"
                    prefetch={false}
                  >
                    About
                  </Link>
                  <Link
                    href="/contact"
                    className="text-gray-600 hover:text-blue-500"
                    prefetch={false}
                  >
                    Contact
                  </Link>
                </div>
                <Suspense fallback={<HongKongWeatherSkeleton />}>
                  <HongKongWeather />
                </Suspense>
              </div>
            </nav>
          </header>
          {children}
          <footer className="w-full bg-gray-800 text-white py-6">
            <div className="container mx-auto text-center">
              <p>&copy; 2023 MyBlog. All rights reserved.</p>
            </div>
          </footer>
        </main>
      </body>
    </html>
  );
}
